package floorplans;

import processing.core.PApplet;
import processing.core.*;

public class Label{
	  public String name;
	  //type e' R o C
	  public String type;
	  public int r;
	  public int g;
	  public int b;
	  public int x;
	  public int y;
	  public int shape;
	  public boolean over;
	  public boolean selected;
	  
	  
	  Label ( String  name, String type, int r, int g, int b, int shape, int x, int y) {
	    this.name = name ;
	    this.type = type ;
	    this.r = r;
	    this.g = g;
	    this.b = b;
	    this.shape = shape;
	    this.x = x;
	    this.y = y;
	    this.over = false;
	    this.selected = false;
	  }
	  
	  boolean occupied(int x,int y) {
	    if ( Math.sqrt(Math.pow(this.x-x,2) + Math.pow(this.y-y,2)) < shape )
	      return true;
	    else 
	      return false;
	  }
	  void setX(int x) {
	    this.x = x;
	  }
	  
	  void switch_val() {
	    this.selected = !this.selected;
	  }
	  void setY(int y) {
	    this.y = y;
	  }
	  
	  // CHecking if mouse is over the Bubble
	  void rollover(float px, float py) {
	    float d = dist(px,py,x,y);
	    if (d < shape/2) {
	      over = true; 
	    } else {
	      over = false;
	    }
	  }
	  
	  // Display the Bubble
	  void display() {
	    if (over) 
	        stroke(255);
	    else 
	        stroke(0);
	    if (selected) 
	        stroke(180);
	    fill(r,g,b);
	    ellipse(x,y,shape,shape);
	    fill(0);
	    textAlign(CENTER);
	    text(name,x,y+shape/2+20);

	  }
	}